#include<iostream>
using namespace std;
// Основне статистике
int main() {
/*

broj1 + broj2 + broj3 + broj4 // zbir, suma, ...

zbir = broj1; // zbir se akumulira
...
zbir = zbir + broj2;
...
zbir = zbir + broj3;
...




zbir = 0; // od nule
zbir = zbir + broj1;
zbir = zbir + broj2;
...



proizvod = 1; // neutralni el. za mnozenje
proizvod = proizvod * broj1;
proizvod = proizvod * broj2;
...


// min od 4 broja
m = min(min(min(broj1, broj2), broj3), broj4)

minimum = broj1; // prvi je najmanji
minimum = min(minimum, broj2); // ako je drugi manji...
minimum = min(minimum, broj3); // ...
...



minimum = broj1; // short if
minimum = broj2 < minimum ? broj2 : minimum;
minimum = broj3 < minimum ? broj3 : minimum;
...



minimum = broj1; // if
if (broj2 < minimum)
   minimum = broj2;
else
   minimum = minimum;
if (broj3 < minimum)
   minimum = broj3;
else
   minimum = minimum;
...



m = min({m1, m2, m3, m4, m5});

*/    


}