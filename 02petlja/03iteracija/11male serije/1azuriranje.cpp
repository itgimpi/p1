#include<iostream>
using namespace std;
// Ажурирање вредности променљивих
int main() {
/*
x = 1; // azuriranje / update / promena vrednosti prom.
cout << x << endl;
x = 2;
cout << x << endl;





int x = 5; // update, warning!!! ++
cout << x << endl;
cout << x++ << endl;
cout << x << endl;
cout << ++x << endl;






int PovecajZaDva(int x) { // update using funcion
    return x + 2; } // <- the most important

int main() {
    int x = 0;
    cout << "Pocetna vrednost x je " << x << endl;
    x = PovecajZaDva(x);
    cout << "Nova vrednost x je " << x << endl;
    return 0; }





void PovecajZaDva(int x) { // update only in function, not in main!!!
   cout << "Vrednost x na ulazu u funkciju je " << x << endl;
   x = x + 2;
   cout << "Vrednost x na izlazu iz funkcije je " << x << endl; }

int main() {
    int x = 0
    cout << "Pocetna vrednost x je " << x << endl;
    PovecajZaDva(x);
    cout << "Nova vrednost x je " << x << endl;
}

void PovecajZaDva(int& x) { // update in main, using addresses
   x = x + 2; }

int main() { 
   int x = 0
   cout << "Pocetna vrednost x je " << x << endl;
   PovecajZaDva(x);
   cout << "Nova vrednost x je " << x << endl; }




void razmeni1(int a, int b) { // uncorrect swap 
   int tmp = a;
   a = b;
   b = tmp;
}

void razmeni2(int& a, int& b) { // correct swap 
   int tmp = a;
   a = b;
   b = tmp;
}




*/

    return 0;  
}